{
  "task_name": "mark_all_as_read_api",
  "task_description": "# Mark All Notifications as Read API\n\n## Background\nMagicBell provides a REST API to manage notifications. You need to write a simple shell script to mark all notifications as read for a specific user using the MagicBell API.\n\n## Requirements\n- Create a shell script at `/home/user/mark_read.sh`.\n- The script must make a `POST` request to `https://api.magicbell.com/notifications/read`.\n- It must include the `X-MAGICBELL-API-KEY` header, reading its value from the `MAGICBELL_API_KEY` environment variable.\n- It must include the `X-MAGICBELL-USER-EMAIL` header, reading its value from the `MAGICBELL_USER_EMAIL` environment variable.\n- The script must output the raw response from the API to stdout.\n\n## Constraints\n- Project path: `/home/user`",
  "truth": "# Verification Plan\n\n## Setup\n1. `cd /home/user`\n2. Ensure `mark_read.sh` is executable.\n\n## Verification Steps\n1. Run `export MAGICBELL_API_KEY=dummy_key` and `export MAGICBELL_USER_EMAIL=test@example.com`.\n2. Run `bash mark_read.sh`.\n3. Verify that the output contains the MagicBell API error `\"incorrect_api_key\"`. This confirms the request was correctly routed to the MagicBell API with the proper endpoint and headers.\n\n## Expected Results\n- The file `/home/user/mark_read.sh` exists.\n- Running the script with dummy credentials outputs a JSON response from MagicBell containing `\"incorrect_api_key\"`."
}