[environment]
image = "harbor-task-jwt-auth-error-handling"
build_context = "environment"

[execution]
start_command = "cd /home/user/magicbell-app && npm run dev"

[verifier]
dependencies = [
    "requests",
    "PyJWT"
]
